name: Tests
on: push
jobs:
  tests-0:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Changing directory
      run: cd 0
    - name: Install modules
      run: npm install
    - name: Run tests
      run: npm run test